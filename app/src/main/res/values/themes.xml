<?xml version="1.0" encoding="utf-8"?>
<!-- SaveAt — thème naturel (fond chaud, accents verts) -->
<resources>

    <style name="Theme.SaveAt" parent="Theme.MaterialComponents.Light.NoActionBar">
        <!-- Couleurs de base -->
        <item name="colorPrimary">@color/accent_primary</item>
        <item name="colorPrimaryVariant">@color/accent_light</item>
        <item name="colorOnPrimary">@color/bg_surface</item>

        <item name="colorSecondary">@color/accent_lime</item>
        <item name="colorSecondaryVariant">@color/accent_light</item>
        <item name="colorOnSecondary">@color/text_h1</item>

        <item name="android:colorBackground">@color/bg_main</item>
        <item name="colorSurface">@color/bg_surface</item>
        <item name="colorOnBackground">@color/text_body</item>
        <item name="colorOnSurface">@color/text_body</item>

        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:windowLightStatusBar">true</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowLightNavigationBar">true</item>

        <!-- TextInput -->
        <item name="textInputStyle">@style/Widget.MaterialComponents.TextInputLayout.OutlinedBox</item>
    </style>

    <style name="Theme.SaveAt.Splash" parent="Theme.SaveAt">
        <item name="android:windowBackground">@color/accent_primary</item>
        <item name="android:statusBarColor">@color/accent_primary</item>
    </style>

</resources>
